
check entry main(args)
{
	wad = wadfile(args[0]);
	map = map::view(wad, wad->wadentry(0).name);
	close(wad);
	
	writer = cswopen(STDOUT());
	each (element, data : map->map::elements())
		writer->map::writeudmfelement(element.type, data)->flush();
	
	close(writer);
}